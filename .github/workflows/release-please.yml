name: Release please

on:
  push:
    branches: [ main ]

jobs:
  release-please:
    runs-on: ubuntu-18.04

    steps:
      - uses: actions/checkout@v3

      - name: Install release-please
        run: npm install --global release-please@13.14.0
      
      - name: Run release-please
        shell: bash
        run: |
          release-please --debug release-pr --token="${{ github.token }}" --repo-url="CBenoit/actions-tests"

